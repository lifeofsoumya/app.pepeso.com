const excludedUrls = {
    header: ['/excluded-header', '/features'],
    footer: ['/excluded-footer', '/features'],
    both: [],
};

const secondaryHeaderUrls = [
    '/features', '/apply'
]

export {excludedUrls, secondaryHeaderUrls}